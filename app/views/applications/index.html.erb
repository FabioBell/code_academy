<% @applications.each_slice(4) do |array_application| %>
	<div class="row">
		<% array_application.each do |application|%>
			<div class="col-sm-3">
				<%= link_to application.name, application_path(application.id)%>
			</div>
		<%end%>
	</div>	

<%end%>
